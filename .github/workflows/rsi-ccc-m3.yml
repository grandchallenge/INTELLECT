name: RSI-CCC M3 proposer/evidence independence

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - "lean/RSICCC.lean"
      - "lean/RSICCCM1.lean"
      - "lean/RSICCCM2.lean"
      - "lean/RSICCCM2Receipts.lean"
      - "lean/RSICCCM3.lean"
      - "lean-toolchain"
      - "lakefile.toml"
      - "governance/rsi_ccc_wp00/formal_object.json"
      - "governance/rsi_ccc_wp00/manifest.json"
      - "governance/rsi_ccc_wp00/m3_proposer_evidence.json"
      - "src/grand_intellect/rsi_ccc_wp00.py"
      - "src/grand_intellect/rsi_ccc_wp00_m2_receipts.py"
      - "src/grand_intellect/rsi_ccc_wp00_m3.py"
      - "src/grand_intellect/rsi_ccc_wp00_m3_proposer.py"
      - "tests/test_rsi_ccc_wp00_m3.py"
      - ".github/workflows/rsi-ccc-m3.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: intellect-rsi-ccc-m3-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  m3:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false

      - name: Install pinned elan bootstrap
        shell: bash
        run: |
          set -euo pipefail
          url="https://raw.githubusercontent.com/leanprover/elan/0e36a07b9bbcc5381fa6250df109f9a4f94d7bac/elan-init.sh"
          curl -fsSL "$url" -o /tmp/elan-init.sh
          python - <<'PY'
          from hashlib import sha1
          from pathlib import Path
          data = Path('/tmp/elan-init.sh').read_bytes()
          got = sha1(f"blob {len(data)}\0".encode('ascii') + data).hexdigest()
          expected = 'ab8c346be2d665b2c77a6eba0dc2338c43413a9c'
          assert got == expected, (got, expected)
          PY
          sh /tmp/elan-init.sh -y --default-toolchain none --no-modify-path
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"

      - name: Install pinned Lean toolchain
        shell: bash
        run: |
          set -euo pipefail
          toolchain="$(cat lean-toolchain)"
          elan toolchain install "$toolchain"
          elan default "$toolchain"
          lean --version
          lake --version

      - name: Reject proof placeholders and local axioms
        shell: bash
        run: |
          set -euo pipefail
          if grep -R -nE '(^|[[:space:]])(sorry|admit|axiom)[[:space:]]' lean --include='*.lean'; then
            echo "Proof placeholder or local axiom detected" >&2
            exit 1
          fi

      - name: Build retained M1 dependencies
        shell: bash
        run: |
          set -euo pipefail
          lake build

      - name: Compile retained M2 and M3 without mutating the M1 Lake manifest
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .lake/build/lib/lean
          lake env lean -o .lake/build/lib/lean/RSICCCM2.olean -i .lake/build/lib/lean/RSICCCM2.ilean lean/RSICCCM2.lean
          lake env lean -o .lake/build/lib/lean/RSICCCM2Receipts.olean -i .lake/build/lib/lean/RSICCCM2Receipts.ilean lean/RSICCCM2Receipts.lean
          lake env lean -o .lake/build/lib/lean/RSICCCM3.olean -i .lake/build/lib/lean/RSICCCM3.ilean lean/RSICCCM3.lean

      - name: Replay M1 through M3 compiled modules with Lean kernel checker
        shell: bash
        run: |
          set -euo pipefail
          lake env leanchecker RSICCC RSICCCM1 RSICCCM2 RSICCCM2Receipts RSICCCM3

      - name: Replay exact M3 proposer/evaluator evidence
        shell: bash
        run: |
          set -euo pipefail
          PYTHONPATH=src python -m unittest tests.test_rsi_ccc_wp00_m3 -v
